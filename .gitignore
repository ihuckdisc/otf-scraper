# clasp auth (never commit)
.clasprc.json

# Easy-template clasp target (Script ID local-only; see .clasp.easy.json.example)
.clasp.easy.json

# OS noise
.DS_Store

# Local node deps (only the dev harness uses node; no deps required)
node_modules/

# Fixtures: local samples + dev-only docs; track distribution markdown only
fixtures/*
!fixtures/DASHBOARD_REFERENCE.md
!fixtures/USER_GUIDE.md
!fixtures/OWNER_EASY_TEMPLATE_RUNBOOK.md
